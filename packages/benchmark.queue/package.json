{
  "name": "@nodelib/benchmark.queue",
  "version": "0.0.0",
  "description": "The queue converter for @nodelib/benchmark",
  "license": "MIT",
  "repository": "https://github.com/nodelib/nodelib/tree/master/packages/benchmark.queue",
  "keywords": [
    "NodeLib",
    "benchmark",
    "bench",
    "queue",
    "converter"
  ],
  "engines": {
    "node": ">= 6"
  },
  "main": "out/index.js",
  "typings": "out/index.d.ts",
  "scripts": {
    "clean": "rimraf out",
    "lint": "tslint \"src/**/*.ts\" -p . -t stylish",
    "compile": "tsc -p .",
    "test": "mocha \"out/**/*.spec.js\" -s 0",
    "build": "npm run clean && npm run lint && npm run compile && npm test",
    "watch": "npm run clean && npm run compile -- --sourceMap --watch"
  },
  "dependencies": {
    "@nodelib/benchmark.client": "^1.0.0"
  }
}
