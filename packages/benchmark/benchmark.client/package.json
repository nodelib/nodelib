{
  "name": "@nodelib/benchmark.client",
  "version": "2.0.1",
  "description": "A «client» side code for @nodelib/benchmark",
  "license": "MIT",
  "repository": "https://github.com/nodelib/nodelib/tree/master/packages/benchmark/benchmark.client",
  "keywords": [
    "NodeLib",
    "benchmark",
    "bench",
    "client"
  ],
  "engines": {
    "node": ">= 6"
  },
  "main": "out/index.js",
  "typings": "out/index.d.ts",
  "scripts": {
    "clean": "rimraf out",
    "lint": "tslint \"src/**/*.ts\" -p . -t stylish",
    "compile": "tsc -b .",
    "compile:watch": "tsc -p . --watch --sourceMap",
    "test": "mocha \"out/**/*.spec.js\" -s 0",
    "build": "npm run clean && npm run lint && npm run compile && npm test",
    "watch": "npm run clean && npm run compile:watch"
  },
  "dependencies": {
    "@nodelib/benchmark.meter": "1.0.2"
  }
}
