{
  "name": "@nodelib/benchmark.worker",
  "version": "1.0.1",
  "description": "A worker that runs iterations of races for @nodelib/benchmark",
  "license": "MIT",
  "repository": "https://github.com/nodelib/nodelib/tree/master/packages/benchmark/benchmark.worker",
  "keywords": [
    "NodeLib",
    "benchmark",
    "bench",
    "worker"
  ],
  "engines": {
    "node": ">= 6"
  },
  "main": "out/index.js",
  "typings": "out/index.d.ts",
  "scripts": {
    "clean": "rimraf out",
    "lint": "tslint \"src/**/*.ts\" -p . -t stylish",
    "compile": "tsc -b .",
    "compile:watch": "tsc -p . --watch",
    "test": "mocha \"out/**/*.spec.js\" -s 0",
    "build": "npm run clean && npm run lint && npm run compile && npm test",
    "watch": "npm run clean && npm run compile:watch"
  },
  "dependencies": {
    "@nodelib/benchmark.client": "^2.0.0",
    "@nodelib/benchmark.meter": "^1.0.1",
    "@nodelib/benchmark.queue": "^1.0.2"
  }
}
